import React from 'react';
import axios from 'axios';

const url = 'localhost:8000/accounts/'
// Add token/ for obtaining a token pair
// Add create/ to create a user

class Login extends React.Component {
    render() {
        return (
            <p>A Login form</p>
        )
    }
}

export default Login;